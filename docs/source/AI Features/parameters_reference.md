# Parameters Reference

Compact lookup table for every `Parameters` class attribute in `diskmint.model`.
For each parameter: physical meaning, units, default value, valid range, and key interactions.

*This page is under construction — content will be added in an upcoming release.*

See {doc}`../Quick Start/quickstart` for the parameter CSV format and a working example.
